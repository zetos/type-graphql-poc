# WIP
# DONT RUN IT YET

# Check docker version
docker-compose -v

# Start up containers in the background (when its already built)
docker-compose up -d

# Check docker contrainers
docker container ls

# Check docker-compose logs
docker-compose logs

# Shutdown the containers
docker-compose down